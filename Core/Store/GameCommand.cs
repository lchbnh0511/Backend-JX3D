namespace BackendJX3D.Core.Store;

public static class GameCommand
{
    public static readonly TimeSpan Timeout = TimeSpan.FromSeconds(3);
    public static readonly TimeSpan ItemSettle = TimeSpan.FromMilliseconds(200);
}
